# Causal graph sources

Published causal graph JSON files are stored here. Jekyll ignores underscore-
prefixed directories, while the matching static SVG files under
`assets/causal-graphs/` are published with the site.
